<div class="comments">
  {{#each this}}
  <section class="comment ">
    <div class="bg-brown">
      <a href="default.asp" target="_blank"> {{user.username}} </a> on {{format_date created_at}}
    </div>
    <div class="text  wrap">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>